#include <stdio.h>

int main (){
	
	int x;
	
	printf("Digite um numero:\n");
	scanf("%d", &x);
	
	if(x>0)
		printf("Numero positivo");
	else
		if(x<0)
		printf("Numero negativo");
		else
		printf("O numero eh zero");	

return 0;
}
