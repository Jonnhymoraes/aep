#include <stdio.h>
#include <math.h>

int main (){
	int i,num,result,a,b;

	printf("informe um valor:\n");
	scanf("%d",&num);
	 a = 0;
     b = 1;
 
    for(i = 0; i < num-1; i++){
    	result = a + b;
    	a = b;
    	b = result;
  	}	
    printf("%d = %d\n",num,result);
	return 0;
}
