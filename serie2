//Exercicio 3
#include <stdio.h>
#include <math.h>

void main(){
	int y,x,n,m;
	
	printf("Digite N:");
	scanf("%d", &n);
	printf("Digite X:");
	scanf("%d", &x);
	
	y=0;
	for(m=2;m<=n;m=m+2){
		y=m*pow(x,m)+y;
	}
	printf("Resultado:%d", y);
}
